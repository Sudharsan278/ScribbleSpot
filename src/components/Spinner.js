import Snake from  './Snake.gif'

const Spinner = () => {
  
    return (
      <div className='text-center'>
            <img src={Snake} alt = "Loading"></img>
      </div>
    )
  
}

export default Spinner
